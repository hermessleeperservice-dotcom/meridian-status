# Follow-up — 2026-07-08 19:06 BST

## Acknowledged
- Poll-worker dedup fix (commit 2cfd192e) received and confirmed applied.

## Problem: fix didn't actually stop the duplication
The 18:00 BST auto-ack run (commit 4fa7060f) re-created `loop/` files for the 1106, 1136, 1206, 1306, 1336, 1406, 1506, 1536, 1736, 1808 follow-ups plus all the June inbox items — all of which already had companions from the earlier 18:39 run (commit 5f219e03). Same duplicate-ack behaviour as before the fix, just excluding whichever couple of items happened to be produced in the immediately-prior run (1236, 1836).

**Likely cause:** every generated `loop/` filename is prefixed with the *current run's own timestamp* (e.g. `loop/2026-07-08-1900-2026-07-08-1106-foll...`). Comparing `os.path.basename(f)` of a freshly-built candidate against `known_loop_files` will never match, because the candidate always carries this run's unique prefix baked in — the basenames can't collide by construction, regardless of whether the source inbox file was already acknowledged in a previous run.

**Correct fix:** dedup needs to key off the *source* token — the inbox filename/timestamp embedded after the run-prefix — not the full generated filename. Strip the leading `YYYY-MM-DD-HHMM-` run-prefix from both the new candidate and every existing `loop/` entry, then compare what remains.

## Instruction
Apply the corrected logic if there's time left this cycle. If not: stop — this is repo clutter, not a functional risk, and isn't worth more than one more attempt tonight. Don't burn further cycles chasing it. It'll be noted in the 22:30 wrap-up for Tomasz either way.

## Standing holds — unchanged
- Finance Bot OAuth: repo confirmed not on this machine, blocker already flagged once for Tomasz — do not re-raise.
- Commit e262a9b3 (`run_console`): untouched, do not push or run.

## Next
Loop ends ~22:30 BST.
