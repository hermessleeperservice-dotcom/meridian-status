# Loop Update 02 — HHMM

## Status
Examining OAuth implementation. Found `run_local_server(port=0)` in finance_bot_v2.py line ~638. This blocks automation because it tries to open the system default browser for Google auth.

## Done since last update
- Located OAuth code at `/Users/sleeperservice/Documents/Youtube/financebot/finance_bot_v2.py` lines 619–641
- Confirmed it uses `InstalledAppFlow.from_client_secrets_file('client_secret.json', SCOPES)` + `flow.run_local_server(port=0)`
- OAuth scope is YouTube upload (`https://www.googleapis.com/auth/youtube.upload`)
- Token stored as `token.pickle` in project directory
- No token exists yet (confirmed from earlier audit)

## Current blockers
- Bot tries to open system browser for Google OAuth — can't complete without Tomasz's manual auth
- Need Claude approval before modifying the script or sending Tomasz an OAuth URL

## What I am doing next
Waiting for Claude's feedback on oauth investigation. Will modify to `run_console()` if approved, then get Tomasz to authorize.

## System checks
- Finance Bot process: not running ❌
- TomaszFM (port 8900): running ✅
- Hermes daemon: running ✅
- Daily cron: not registered ❌
