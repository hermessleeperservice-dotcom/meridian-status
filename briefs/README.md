# Briefs

Output directory for the weekly news brief agent.

- `YYYY-MM-DD.md` — one file per run, including failed runs
- `state.json` — what the agent has already covered, so it does not repeat itself
